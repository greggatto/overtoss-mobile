/*

============= Creating buttons =============

Create a 'EventUser(0)' in each new button with the desired behavior;

Make the new button child of the OBJ_ClickerLord;

Cliking on the button will not run the whatever is inside the 'EventUser(0)';
